{
  "name": "mfi",
  "private": true,
  "version": "0.0.0",
  "description": "m.fi — intent-based credit on Monad (coincidence of agents for lending/borrowing)",
  "scripts": {
    "dev": "pnpm --filter @mfi/app dev",
    "dev:server": "pnpm --filter @mfi/server dev",
    "dev:all": "pnpm --parallel --filter @mfi/app --filter @mfi/server dev",
    "build": "pnpm --filter @mfi/app build"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "viem": "^2.21.0"
  },
  "packageManager": "pnpm@10.5.2"
}
