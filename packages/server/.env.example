# Privy server credentials (the secret is private — never commit the real .env)
PRIVY_APP_ID=
PRIVY_APP_SECRET=

# Enables the real Claude negotiating solver. Without it, Atlas uses a deterministic fallback.
ANTHROPIC_API_KEY=

CHAIN=monad
PORT=8787
