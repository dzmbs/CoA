# Privy app id — create one at https://dashboard.privy.io (free). Required for wallet connect.
VITE_PRIVY_APP_ID=

# Which network the app targets: "monad" (mainnet 143) or "monadTestnet" (10143)
VITE_CHAIN=monad

# Off-chain orderbook / solver service
VITE_API_URL=http://localhost:8787
